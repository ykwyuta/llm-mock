# セキュリティポリシー

## このソフトウェアの位置づけ

llm-mock は**開発とテストのためのモックサーバー**です。本番環境や、信頼できない
ネットワークに公開することを想定していません。既定の状態で次のようになっています。

| 項目 | 既定の挙動 |
|---|---|
| 管理 API (`/__admin`) | **認証なし**。誰でもスタブの登録・リクエストログの閲覧ができます |
| H2 コンソール (`/__admin/h2`) | **有効**。ブラウザから DB を直接操作できます |
| プロバイダー API の認証 | **無効** (`llm-mock.require-auth: false`)。有効にしても**ヘッダーの有無しか見ません** |
| SigV4 署名 | **検証しません** (Bedrock では `Authorization` の有無のみ) |

**ローカルホスト、CI のジョブ内、閉じた開発ネットワークでのみ使ってください。**
公開ネットワークに置くと、リクエストログに残ったプロンプト本文が誰でも読める状態になります。

`PROXY` / `CACHED_PROXY` モードで本物の API 資格情報を設定した場合、その資格情報を持った
サーバーが認証なしの管理面を晒すことになる点にもご注意ください。

## 記録ファイルの取り扱い

`PROXY` / `CACHED_PROXY` が書き出す `recordings/*.json` について:

- **資格情報は自動的に伏せられます。** `Authorization` / `x-api-key` / `x-goog-api-key` /
  `api-key` / `proxy-authorization` / `cookie` / `set-cookie` / `x-amz-security-token` の各
  ヘッダーと、`?key=` / `?access_token=` のクエリは `REDACTED` に置換されます
  (`llm-mock.proxy.redact-headers` / `redact-query-params` で追加できます)。
- **プロンプト本文と応答本文はそのまま残ります。** 機密情報を含む会話を記録した場合は、
  リポジトリにコミットする前に中身を確認してください。
- リクエストログ (`/__admin/requests`) にもリクエスト本文が残ります。こちらはインメモリで、
  プロセスを終了すると消えます。ヘッダーは記録していません。

## 脆弱性の報告

**公開 Issue には書かないでください。**
GitHub の [Security Advisories](https://github.com/ykwyuta/llm-mock/security/advisories/new)
から非公開で報告してください。可能であれば次を含めてください。

- 再現手順、影響を受けるバージョン (コミットハッシュ)
- 想定される影響 (情報漏洩・任意コード実行など)

内容を確認のうえ、修正方針を返信します。

## 対象範囲

上の「位置づけ」に書いたとおり、**管理 API に認証が無いこと、H2 コンソールが有効なこと、
SigV4 を検証しないことは仕様**であり、脆弱性としては扱いません。
一方、次のようなものは報告の対象です。

- 記録ファイルやログに、伏せられるはずの資格情報が残ってしまう
- リクエスト内容によって、意図しないファイルの読み書きや外部への通信が起きる
- プロキシ先の指定を、設定していないホストに向けさせられる

## サポート対象バージョン

現在は `master` の最新コミットのみをサポートしています。
